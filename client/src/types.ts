export interface announcement{
    title: String,
    message: String,
    announcer: String,
    subject:string,
    createdAt: Date
}
export interface upcomming{
    Type: String,
    Subject: String,
    Topic: String,
    DueOn: Date,
    createdAt: Date
}